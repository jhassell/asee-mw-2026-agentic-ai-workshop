# ASEE PEER papers are licensed for private, per-participant, session-scoped
# use only (permission granted 2026-08-26). They must NEVER be committed to
# this public repository. They arrive at setup time from the private corpus
# repo. Do not remove these lines; do not `git add -f` them.
corpus/papers/
corpus/*.tar.gz
*.tar.gz

# Local secrets — nothing with a key in it belongs in git.
.env
*.key
workshop-key.txt

# Junk
__pycache__/
.DS_Store
/tmp/

# Takeaway (d) is NOT ready to ship. It carries a facilitator-only HTML comment
# (visible to participants as raw markdown in the editor) and five unresolved
# [I-1]..[I-5] items needing John's decisions. It was committed by accident on
# 2026-09-09 via `git add exercises/` and removed the same evening. Ignored here
# so that cannot happen again. Delete these three lines when the file is ready.
